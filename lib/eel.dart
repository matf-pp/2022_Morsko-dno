import 'package:flutter/material.dart';

class MyEel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset(
        'lib/slike/eel.gif',
        height:250,
        width:250,
      ),
    );
  }
}